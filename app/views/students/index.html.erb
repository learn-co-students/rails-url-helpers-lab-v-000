<div>
  <%@students.each do |student|%>
     <div>
      <%= link_to "#{student.to_s}", student_path(student)%></div>
      <div><%= link_to "Activate Student", activate_student_path(student)%>

    </div>
  <%end%>
</div>
