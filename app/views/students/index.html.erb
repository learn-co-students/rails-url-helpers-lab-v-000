<div>
  <% @students.each do |student| %>
  <!--  <div><%= "/students/#{student.id}" %><%= student.to_s %></div> -->
    <div><%= link_to "post", "/post/#{post.id}" %></div>
    <div><%= student.first_name + " " + student.last_name %></div>
  <% end %>
</div>
