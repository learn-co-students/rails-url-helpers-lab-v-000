<div>
  <% @students.each do |student| %>

    <p>
      <%=link_to student.to_s, "/students/#{student.id}"%>
    </p>

    <p>
      <%=link_to(student.to_s, student_path(student.id))%>
    </p>

    <p>
      <%=link_to(student.to_s, student_path(student))%>
    </p>

  <% end %>
</div>
