<%if @student.active%>
  <% @student.active = false %>
  <% @student.save %>
<% else %>
  <% @student.active = true %>
  <% @student.save%>
<% end %>
<% controller.redirect_to student_path(@student) %>