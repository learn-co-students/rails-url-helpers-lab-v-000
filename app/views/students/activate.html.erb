<% if @student.active == true %>
  <% @student.active = false %>
  <% @student.save %>
  <% controller.redirect_to student_path(@student) %>
<% else %>
  <% @student.active = true %>
  <% @student.save %>
  <% controller.redirect_to student_path(@student) %>
<% end %>
