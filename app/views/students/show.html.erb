<h1><%=@student.to_s%></h1>

  <% if @student.active? %>
    <%= "This student is currently active." %>
  <% else %>
    <%= "This student is currently inactive." %>
  <% end %>

<div><%= link_to("Change Status", activate_path) %></div>
