<h1><%= @student.to_s %></h1>
  <% if @student.active == true %>
  <%= "This student is currently active." %>
  <% else %>
  <%= "This student is currently inactive." %>
  <% end %>
  <%= link_to '<button type="button">Change Status</button>'.html_safe, activate_student_path %>
